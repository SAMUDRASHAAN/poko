# Poko game engine

Pure, deterministic Dart gameplay core with zero runtime dependencies. The
foundation freezes `Num`, data/action types, and public entry-point signatures;
the Phase 1F engine worktree supplies implementations that must match the pinned
TypeScript oracle through `contracts/` fixtures.
